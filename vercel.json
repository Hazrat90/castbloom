{
  "version": 2,
  "buildCommand":npm install -D typescript @types/react @types/react-dom

  "installCommand": "npm install",
  "outputDirectory": "dist",
  "framework": "vite",
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}


